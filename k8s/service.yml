apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: opentraum
  labels:
    app: web
    app.kubernetes.io/part-of: opentraum
spec:
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
